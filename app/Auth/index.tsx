import { AuthProvider, useAuthApp } from "@/context/userContext";
import { MarcoLayout, TextSmall } from "@/utils/utils";
import { useEffect } from "react";
import LoginAuth from "./LoginAuth";

const Auth = () => {
  return <LoginAuth />;
};

export default Auth;
